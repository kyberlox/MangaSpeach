## Используй для запуска с помощью Docker

```
bash start.sh
```
или
```
docker build -t manga-speach-app .
docker run -it -v ./:/app --rm manga-speach-app
```

## Основной способ запуска
```
python main.py
```
